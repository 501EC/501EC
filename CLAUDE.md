# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

This is the **501EC/501EC** GitHub special profile repository. The `README.md` equivalent here is `BIO'S.md`, which GitHub renders as the public profile page for the user `@501EC`.

## Structure

- `BIO'S.md` — The sole file; serves as the GitHub profile README. GitHub automatically displays this on the user's public profile page.

## Conventions

- Changes to `BIO'S.md` are immediately reflected on the public GitHub profile once pushed to `main`.
- The file uses GitHub Flavored Markdown (GFM) and GitHub emoji shortcodes (e.g. `👋`, `👀`).
- The HTML comment block at the bottom (`<!-- ... -->`) is intentional boilerplate from GitHub's profile template and should be preserved or removed only if explicitly requested.

## Interests / Context

The owner (`@501EC`) is learning C and Python, with a strong interest in CTF (Capture The Flag) challenges. When suggesting code examples, tooling, or resources, lean toward beginner-friendly C/Python material and CTF-relevant topics (binary exploitation, reverse engineering, cryptography basics).
